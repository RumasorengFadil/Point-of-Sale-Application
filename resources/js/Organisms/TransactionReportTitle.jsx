import PageTitle from "@/Components/PageTitle";
import React from "react";

const TransactionReportTitle = ({}) => {
    return (
        <PageTitle className="px-4">
            Laporan <span className="font-semibold">Transaksi</span>
        </PageTitle>
    );
};

export default TransactionReportTitle;
